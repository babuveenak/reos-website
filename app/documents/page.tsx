import Link from "next/link";
import { Breadcrumbs, GatewayShell, StatusPill } from "../components/GatewayShell";
import { processSteps } from "../data/gateways";
export const metadata={title:"Controlled Documents · REOS"};
export default function DocumentsPage(){return <GatewayShell><div className="gateway-content"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Documents"}]}/><header className="gateway-page-head compact"><div><span className="gateway-kicker">CONTROLLED DOCUMENT REGISTER</span><h1>One ID. One current version.<br/><em>Every use is traceable.</em></h1><p>These records define the blueprint structure only. No project source file has been loaded or accepted.</p></div><StatusPill tone="warning">{processSteps.length} template records</StatusPill></header><section className="register-table"><table><thead><tr><th>Document ID</th><th>Gateway step</th><th>Version</th><th>Source assurance</th><th>Action</th></tr></thead><tbody>{processSteps.map((step)=><tr key={step.id}><td><strong>{step.outputIds[0]}</strong></td><td>{step.title}</td><td>V{step.version}</td><td><StatusPill tone="blocked">Not loaded</StatusPill></td><td><Link href={`/steps/${step.id}`}>Trace →</Link></td></tr>)}</tbody></table></section></div></GatewayShell>}
