import Link from "next/link";
import { gateways } from "../data/gateways";
import { Breadcrumbs, GatewayMetric, GatewayShell, StatusPill } from "../components/GatewayShell";

export const metadata = { title:"Seven-Gateway Journey · REOS", description:"Navigate the complete REOS development journey from feasibility to a performing asset." };

export default function JourneyPage(){return <GatewayShell><div className="gateway-content"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Journey"}]}/><header className="gateway-page-head"><div><span className="gateway-kicker">YOU ARE HERE · WHOLE JOURNEY</span><h1>One route.<br/><em>Seven controlled decisions.</em></h1><p>Follow the project from opportunity to operating asset. Every accepted output becomes a traceable input to the next gateway.</p></div><div className="gateway-head-metrics"><GatewayMetric label="Route position" value="Overview" note="G1 → G7"/><GatewayMetric label="Template completeness" value="0%" note="No live evidence loaded"/><GatewayMetric label="Critical blockers" value="Not assessed" note="Project data required"/></div></header><section className="journey-route" aria-labelledby="journey-route-title"><h2 id="journey-route-title" className="visually-hidden">Seven gateway route</h2><ol>{gateways.map((gateway,index)=><li key={gateway.id} style={{"--gate-accent":gateway.accent} as React.CSSProperties}><Link href={`/gateway/${gateway.slug}`}><span className="journey-number">{gateway.id}</span><span className="journey-landmark" aria-hidden="true"><i/><b/><u/></span><span className="journey-copy"><small>Gateway {index+1} of 7</small><strong>{gateway.name}</strong><em>{gateway.question}</em><span>{gateway.steps.length} controlled steps</span></span><span className="journey-arrow" aria-hidden="true">→</span></Link></li>)}</ol><div className="journey-return"><strong>User Happiness Assurance</strong><span>Measure outcomes, close recovery actions and return verified lessons to the next G1.</span><Link href="/user-happiness">Open the assurance loop →</Link></div></section><section className="gateway-callout"><div><span className="gateway-kicker">START SAFELY</span><h2>Begin with the evidence, not the answer.</h2><p>This platform is a blueprint-linked process template. Live readiness remains at zero until project owners, current sources and authorized confirmations are loaded.</p></div><StatusPill tone="warning">Template · not a live approval</StatusPill></section></div></GatewayShell>}
