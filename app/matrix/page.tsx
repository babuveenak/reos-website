import { Breadcrumbs, GatewayShell } from "../components/GatewayShell";
import { MatrixExplorer } from "../components/MatrixExplorer";
import { gateways, matrixCells, stakeholderGroups } from "../data/gateways";
export const metadata={title:"7 × 12 Stakeholder Matrix · REOS",description:"Inspect all 84 connections between seven property stages and twelve UAE stakeholder groups."};
export default async function MatrixPage({searchParams}:{searchParams:Promise<{gateway?:string}>}){const {gateway="all"}=await searchParams;return <GatewayShell><div className="gateway-content gateway-content-wide"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"7 × 12 matrix"}]}/><header className="gateway-page-head compact"><div><span className="gateway-kicker">7 STAGES × 12 STAKEHOLDER GROUPS</span><h1>The whole ecosystem.<br/><em>Every relationship visible.</em></h1><p>Select any cell to see how that stakeholder participates at the lifecycle stage, who is accountable and which evidence supports the outcome.</p></div><div className="matrix-legend"><span><i/>Not started</span><span><i/>Non-color state marker</span><strong>All 84 cells are template records—not live approvals.</strong></div></header><MatrixExplorer gateways={gateways} groups={stakeholderGroups} cells={matrixCells} initialGateway={gateway}/></div></GatewayShell>}
