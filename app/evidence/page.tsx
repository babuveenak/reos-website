import Link from "next/link";
import { Breadcrumbs, GatewayShell, StatusPill } from "../components/GatewayShell";
import { processSteps } from "../data/gateways";
export const metadata={title:"Evidence Register · REOS"};
export default function EvidencePage(){return <GatewayShell><div className="gateway-content"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Evidence"}]}/><header className="gateway-page-head compact"><div><span className="gateway-kicker">EVIDENCE REGISTER</span><h1>Show what proves it.<br/><em>Keep the source visible.</em></h1><p>Completeness and factual assurance are different measures. A populated field is not proof that an external fact is true.</p></div><StatusPill tone="blocked">External facts not assessed</StatusPill></header><section className="register-table"><table><thead><tr><th>Evidence ID</th><th>Requirement / step</th><th>Owner</th><th>Status</th><th>Lineage</th></tr></thead><tbody>{processSteps.map((step)=><tr key={step.id}><td><strong>{step.evidenceIds[0]}</strong></td><td>{step.id} · {step.title}</td><td>{step.responsibleOwner}</td><td><StatusPill tone="blocked">Not attached</StatusPill></td><td><Link href={`/steps/${step.id}`}>Source chain →</Link></td></tr>)}</tbody></table></section></div></GatewayShell>}
